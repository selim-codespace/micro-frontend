{
  "builds": [
    {
      "src": "apps/host/package.json",
      "use": "@vercel/next",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "installCommand": "cd apps/host && echo \"NEXT_PRIVATE_LOCAL_WEBPACK=$NEXT_PRIVATE_LOCAL_WEBPACK\" && npm install webpack@^5.103.0 || echo 'webpack install skipped'",
  "buildCommand": "cd apps/host && npm run vercel-build",
  "env": {
    "NEXT_PRIVATE_LOCAL_WEBPACK": "true"
  }
}